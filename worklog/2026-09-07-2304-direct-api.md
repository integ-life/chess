# Direct API migration

User chose to keep static frontends on GitHub Pages and call each backend hostname directly, retiring Cloudflare Worker routing.

Preserve exact CORS allowlists and host-only secure cookies. Change OAuth callbacks together with existing Auth client configuration, without changing credentials or permissions. Validate production before removing old routes.

Status: implementation and verification in progress.

Production verification completed: public frontend metadata is clean and served JS uses the direct API hostname. Direct origin responses and relevant CORS preflights passed; actual signed-in browser reads passed. All 11 former Worker route assignments were disabled, preserving scripts/patterns for rollback. Chat/Things OAuth MCP clients must reconnect to their API-host resource URLs.

# 统一登录语言传递

- 目标：从国际象棋进入中心认证时，登录页沿用当前界面语言。
- 实现：前端登录按钮携带 locale；后端经过长度约束后转发为 OAuth `ui_locales`。
- 验证：聚焦 `go test ./internal/api` 与前端生产构建通过。仓库既有全量测试仍因国际象棋分支中残留的中国象棋 FEN/规则用例失败，与本次 locale 参数改动无关；待完成提交、部署和生产跳转验收。
